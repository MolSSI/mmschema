name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - jsonschema
    - codecov
    - pydantic
    - qcelemental
    - git+https://github.com/MolSSI/cmselemental.git@dev
    - git+https://github.com/MolSSI/mmelemental.git
    - sphinx
    - sphinx-material
